# =============================================================================
# KANNA Project - Python Dependencies
# =============================================================================
# Installation: pip install -r requirements.txt
#
# For conda users, install RDKit separately:
#   conda install -c conda-forge rdkit
# =============================================================================

# Core scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Data visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Machine learning
scikit-learn>=1.2.0
xgboost>=1.7.0
lightgbm>=3.3.0

# Deep learning (optional, comment out if not using GPU)
# torch>=2.0.0
# tensorflow>=2.12.0

# Cheminformatics
# rdkit>=2023.3.1  # Install via conda instead: conda install -c conda-forge rdkit

# Chemical deep learning
# deepchem>=2.7.0  # Uncomment if doing neural network-based QSAR

# NLP & Text mining
spacy>=3.5.0
# python -m spacy download en_core_web_sm  # Run after installing spacy
sentence-transformers>=2.2.0
bertopic>=0.15.0

# Model interpretation
shap>=0.41.0
lime>=0.2.0

# Jupyter & interactive analysis
jupyter>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.0.0
notebook>=6.5.0

# Data access & APIs
requests>=2.28.0
beautifulsoup4>=4.11.0
biopython>=1.81  # For sequence analysis if needed
pubchempy>=1.0.4  # PubChem API

# PDF extraction & document parsing (MinerU)
# IMPORTANT: Package renamed from magic-pdf to mineru in v2.0+
# Installation: pip install uv && uv pip install -U "mineru[core]"
# mineru[core]>=2.0.0  # MinerU - Extract formulas, tables, text from scientific PDFs
# NOTE: Install separately via uv (not via requirements.txt) - see Guide 6

# Molecular modeling utilities
# pymol-open-source>=2.5.0  # Molecular visualization
# openbabel>=3.1.1  # Format conversion (install via conda)

# Statistical analysis
statsmodels>=0.14.0
pingouin>=0.5.3  # Statistics library

# File formats
openpyxl>=3.1.0  # Excel files
xlrd>=2.0.1  # Old Excel format
h5py>=3.8.0  # HDF5 files

# Progress bars & utilities
tqdm>=4.65.0
joblib>=1.2.0

# Configuration & environment
python-dotenv>=1.0.0
pyyaml>=6.0

# Quality & testing (development)
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0
mypy>=1.3.0

# =============================================================================
# INSTALLATION NOTES
# =============================================================================
#
# 1. Create virtual environment:
#    python -m venv venv
#    source venv/bin/activate  # Linux/Mac
#    venv\Scripts\activate     # Windows
#
# 2. Upgrade pip:
#    pip install --upgrade pip
#
# 3. Install dependencies:
#    pip install -r requirements.txt
#
# 4. Install RDKit via conda (recommended):
#    conda create -n kanna python=3.10
#    conda activate kanna
#    conda install -c conda-forge rdkit
#    pip install -r requirements.txt
#
# 5. Download spaCy language model:
#    python -m spacy download en_core_web_sm
#
# =============================================================================
